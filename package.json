{
  "name": "gatsby-demo",
  "version": "1.0.0",
  "private": true,
  "description": "gatsby-demo",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "tinacms build && gatsby develop",
    "dev": "tinacms dev -c \"gatsby develop\"",
    "start": "tinacms build && gatsby develop",
    "build": "tinacms build && gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean"
  },
  "dependencies": {
    "@tinacms/cli": "^1.1.0",
    "gatsby": "^5.6.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "tinacms": "^1.2.0"
  },
  "resolutions": {
    "graphql": "15.8.0"
  }
}
